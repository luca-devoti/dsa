# Binary Tree

- A **full** Binary Search Tree (BST) is a tree where every node has either zero or two children (no node has exactly one child).
- A **complete** Binary Search Tree is a tree where all levels are fully filled except possibly the last level, which is filled from left to right.
